{
  "name": "webpack-example",
  "version": "1.0.0",
  "private": true,
  "description": "Example webpack project using Bundle Badger",
  "scripts": {
    "build": "webpack --mode production",
    "build:stats": "webpack --mode production --json > stats.json",
    "badger": "bundle-badger --stats ./stats.json --dry-run"
  },
  "devDependencies": {
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "bundle-badger": "file:../.."
  }
}
